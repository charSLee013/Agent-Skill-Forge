interface:
  display_name: "Writing Great Skills"
  short_description: "Write predictable agent-consumed documents"
  default_prompt: "Use $writing-great-skills to write or review this agent-consumed document without expanding its authority."
policy:
  allow_implicit_invocation: false
